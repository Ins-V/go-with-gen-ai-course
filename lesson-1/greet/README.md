### Порівняння ручного написання та за допомогою ШІ функції Greet

В версії ШІ краще те що строка збирается через конкатенацію, а не через форматування. Але недолік в тому що він продоблював строку, хоча цього можна не робити.
